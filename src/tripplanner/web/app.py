"""FastAPI application. `/health` and the use-case routes are added in M0 task 7."""

from __future__ import annotations

from fastapi import FastAPI

app = FastAPI(title="AI Travel Itinerary Planner")
